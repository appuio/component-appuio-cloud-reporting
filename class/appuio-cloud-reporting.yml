parameters:
  kapitan:
    compile:
      - input_paths:
          - appuio-cloud-reporting/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - appuio-cloud-reporting/component/main.jsonnet
        input_type: jsonnet
        output_path: ${_instance}/
